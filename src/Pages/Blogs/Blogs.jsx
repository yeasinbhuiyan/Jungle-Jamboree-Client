import useTitle from "../../Hook/UseTitle";


const Blogs = () => {
    useTitle('Blogs')
    return (
        <div>
            <h1>This is blogs section </h1>
        </div>
    );
};

export default Blogs;